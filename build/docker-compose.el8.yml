version: "3.5"
services:
  centos8php74build:
    build:
      context: .
      args:
        centos: $OS8
    image: "aursu/php73build:${OS8TAG}-build"
  centos8php74buildrel:
    build:
      context: .
      dockerfile: Dockerfile.rel
      args:
        centos: $OS8
    image: "aursu/php74build:${OS8TAG}-build-rel"
